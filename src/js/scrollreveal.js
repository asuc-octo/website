import ScrollReveal from 'scrollreveal';

const config = {
  origin: 'bottom',
  duration: 500,
  reset: false,
  delay: 100,
  scale: 0.8,
  easing: 'ease',
  viewFactor: 0.2,
};

let sr = ScrollReveal(config);

export default sr;
