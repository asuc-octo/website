/* eslint-disable */
const config = {
  origin: 'bottom',
  duration: 500,
  reset: false,
  delay: 100,
  scale: 0.8,
  easing: 'ease',
  viewFactor: 0.2,
};

// eslint-disable-next-line no-use-before-define
window.sr = ScrollReveal(config);
